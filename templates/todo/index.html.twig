{% extends 'base.html.twig' %}

{% block title %}Hello TodoController!{% endblock %}

{% block body %}

<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Todo List ✅</h1>
    {% if todo_list | length <= 0 %}
    <p>You have no todos right now</p>
    {% else %}
        <form action="/deleteTodo" method="post">
    {% for todo in todo_list %}
    <p>{{ todo.getName() }}</p>
        <input value="{{ todo.getId() }}">
        <button>Delete</button>
    {% endfor %}
        </form>
    {% endif %}
    <div>
        <form action="/createTodoView" method="get">
    <button>Add todo</button>
        </form>
    </div>
    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ '/var/www/html/PHPCodePractice/SymfonyProjects/demo_project/src/Controller/TodoController.php'|file_link(0) }}">src/Controller/TodoController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/var/www/html/PHPCodePractice/SymfonyProjects/demo_project/templates/todo/index.html.twig'|file_link(0) }}">templates/todo/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}
